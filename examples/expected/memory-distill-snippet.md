# Expected Memory Distill Snippet

A memory distill run after copying `examples/memory/MEMORY.before.md` to `MEMORY.md` should flag the workflow section.

Expected signals:

```text
large-section
many-bullets
workflow-like
operational-detail
```

Exact byte counts will vary.
